july_temperatures = [87, 85, 92, 79, 109]
hot_days = []
if july_temperatures > 90:
    hot_days = [july_temperatures.append(hot_days())]
    print(hot_days)

    #Data we need to receive.
    #1. Total number of votes cast in the election
    #2. List of candidates that received votes
    #3. Total number of votes for each candidate
    #4. Percentage of votes each candidate won
    #5. The winner of the election based on popular vote

# Import the datetime dependency.
import datetime
# Use the now() attribute on the datetime class to get the present time.
now = datetime.datetime.now()
# Print the present time.
print("The time right now is,", now)

    # Assign a variable for the file to load and the path.
#file_to_load = os.path.join("Resources", "election_results.csv")
# Open the election results and read the file.
#with open(file_to_load) as election_data:

    # Print the file object.
     #print(election_data)

# Create a filename variable to a direct or indirect path to the file.
#file_to_save = os.path.join("analysis", "election_analysis.txt")
# Using the open() function with the "w" mode we will write data to the file.
#open(file_to_save, "w")

# Open the election results and read the file.
#with open(file_to_load) as election_analysis:

    # Print the file object.
     #print(election_analysis)